---
title: Cursor
description: Cursor output from ai-rulez generate
---

With the `cursor` preset, `ai-rulez generate` writes `.cursor/rules/*.mdc` (and related Cursor
artifacts). Edit sources under `.ai-rulez/` and `modules/` (for this library), then regenerate.
Do not hand-edit generated files.
